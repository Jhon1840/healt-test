{
  "name": "mlx-e2e-tests",
  "version": "1.0.0",
  "description": "Playwright E2E tests for MLX (login only).",
  "type": "commonjs",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report",
    "test:listings": "npx playwright test --project=listings-only"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.3"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
